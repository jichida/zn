  nginx:
      image: nginx
      restart: always
      expose:
         - "80"
      ports:
          - "80:8090"
      extra_hosts:
          - znserver:172.17.42.1
      volumes:
          - /etc/localtime:/etc/localtime:ro
          - ./nginx.conf:/etc/nginx/conf.d/default.conf
          - /app/zn/deploy/dist:/usr/share/nginx/html
