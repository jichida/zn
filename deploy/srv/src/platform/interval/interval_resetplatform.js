const interval_baseInfoVehicleTotalMile_reset = require('./interval_baseInfoVehicleTotalMile_reset.js');

const inserval_resetplatform = (callbackfn)=>{
  interval_baseInfoVehicleTotalMile_reset(callbackfn);
}

module.exports = inserval_resetplatform;
